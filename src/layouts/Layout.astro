---
import BaseHead from '../components/BaseHead.astro';
import StructuredData from '../components/StructuredData.astro';
import BreadcrumbsSD from '../components/BreadcrumbsSD.astro';

interface Props {
  title: string;
  description: string;
  pubDate: string;
  canonicalURL: string | URL;
  url: string | URL;
}

const { title, description, pubDate, canonicalURL = Astro.url, url } = Astro.props;

---
<html lang="en-GB">
  <BaseHead {title} {description} {canonicalURL} />
  <StructuredData {title} {pubDate} />
  {canonicalURL.href.indexOf("jardim") ? <BreadcrumbsSD url={canonicalURL} /> : null}
  <body>
      <slot />
  </body>
</html>