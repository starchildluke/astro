---
import albums from '../data/albums.json'

const artists = albums.albums.map((album) => (
                    album.artist));

const thingJson = [{'name': ""}]



const item = thingJson.forEach( (thing) => (
  artists.map( (artist)  => 
   thing.name = artist ) )
   )

---
<script type="application/ld+json" set:html={JSON.stringify(thingJson)}></script>