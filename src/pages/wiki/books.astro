---
// COMPONENTS

import BaseHead from '../../components/BaseHead.astro';
import BlogHeader from '../../components/BlogHeader.astro';
import BlogFooter from '../../components/BlogFooter.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import site from '../../data/site.json';
import wiki from '../../data/wiki.json'

// EXPORT

export interface Props {
  post: any;
  metaTitle: string;
  title: string;
  slug: string;
  description: string;
  domain: string;
}

// PROPS

const { post } = Astro.props;

// HOW FETCHING!

const remoteData = await fetch('https://cultrface.co.uk/wp-json/wp/v2/posts?categories=167').then(response => response.json());

const remoteData2 = await fetch('https://distantarcade.co.uk/wp-json/wp/v2/posts?tags=257').then(response => response.json());

const remoteData3 = await fetch('https://sampleface.co.uk/wp-json/wp/v2/posts?tags=3000').then(response => response.json());

const remoteData4 = await fetch('https://logicface.co.uk/wp-json/wp/v2/posts?tags=277').then(response => response.json());

// METADATA VARIABLES

let title = 'Books';
let slug = '/wiki/books/';
let description = 'A wiki page all about books.';
let domain = "https://lukealexdavis.co.uk"

---
<html lang="en">
  <head>
    <BaseHead title={'wiki/' + title.toLowerCase()} description={description} canonicalURL={domain + slug} />
  </head>
  <body>
    <BlogHeader />
    <div class="layout">
      <article class="content">
        <Breadcrumbs title={title}/>
        <section class="intro">
          <h1>{title}</h1>
          <p class="desc">{description}</p>
        </section>
        <section>
          
          <h2 class="wiki-h2">Inner voice</h2>
          
          <ul>
            <li><a href="/post/12-facts-i-didnt-know-about-pokemon/">/12-facts-i-didnt-know-about-pokemon.md</a></li>
            <li><a href="/post/boom-tetris-for-jeff/">/boom-tetris-for-jeff.md</a></li>
            <li><a href="/post/my-new-obsession-is-sega/">/my-new-obsession-is-sega.md</a></li>
            <li><a href="/post/my-five-favourite-video-games-of-all-time/">/my-five-favourite-video-games-of-all-time.md</a></li>
          </ul>
          
        </section>
        
        <section>
          <h2 class="wiki-h2">Out loud</h2>
          <ul class="post-list">
            {remoteData.map((item) => (
            <li class="post-preview wiki-listing post-list">
              <a href={item.link}>{item.title.rendered}</a>
            </li>
            ))}
            {remoteData2.map((item) => (
            <li class="post-preview wiki-listing post-list">
              <a href={item.link}>{item.title.rendered}</a>
            </li>
            ))}
            {remoteData3.map((item) => (
            <li class="post-preview wiki-listing post-list">
              <a href={item.link}>{item.title.rendered}</a>
            </li>
            ))}
            {remoteData4.map((item) => (
            <li class="post-preview wiki-listing post-list">
              <a href={item.link}>{item.title.rendered}</a>
            </li>
            ))}
          </ul>
          <h2 class="wiki-h2">Second opinions</h2>
          
          <ul>
            <li>Coming soon...</li>
          </ul>
          
        </section>
      </article>
      
    </div>
    <BlogFooter></BlogFooter>
  </body>
</html>